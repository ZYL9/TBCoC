{
  "devDependencies": {
    "@vite-pwa/vitepress": "^0.5.0",
    "vitepress": "^1.2.3",
    "fs-extra": "^11.2.0"
  },
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs",
    "prebuild": "node generate-sidebar.js"
  }
}
